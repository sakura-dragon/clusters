apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: vaultwarden
spec:
  hosts:
  - vaultwarden.apps.svc.cluster.local
  - vault.${PUBLIC_DOMAIN}
  gateways:
  - apps-ingress
