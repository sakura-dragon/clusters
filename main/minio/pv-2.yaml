apiVersion: v1
kind: PersistentVolume
metadata:
  name: minio-data-2
  labels:
    app: minio
    minio: data-2
spec:
  capacity:
    storage: 100Gi
  volumeMode: Block
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: minio
  local:
    fsType: xfs
    path: /dev/minio-2/data
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - athena
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-minio-2
  labels:
    app: minio
spec:
  storageClassName: minio
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  selector:
    matchLabels:
      minio: data-2